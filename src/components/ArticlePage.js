import Card from "./Card";
import NoImage from "./NoImage";

const ArticleList = ({ theGuardianResults, loading }) => {
  return (
    <section className="container">
      <section className="sports">
        <h1>ArticleList</h1>
      </section>
    </section>
  );
};

export default ArticleList;